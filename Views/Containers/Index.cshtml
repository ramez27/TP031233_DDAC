@model IEnumerable<CMS.Models.Container>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContainerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContainerHeight)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContainerWidth)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContainerLength)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContainerWeight)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContainerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContainerHeight)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContainerWidth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContainerLength)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContainerWeight)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContainerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContainerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContainerID })
        </td>
    </tr>
}

</table>
